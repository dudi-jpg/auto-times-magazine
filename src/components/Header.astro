---
import { Image } from 'astro:assets';
import { CATEGORIES } from '../consts';
import logoLight from '../assets/logo-light.jpg';

const catHref = (c: string) => `/category/${encodeURIComponent(c)}/`;
const today = new Intl.DateTimeFormat('he-IL', {
  weekday: 'long',
  day: 'numeric',
  month: 'long',
  year: 'numeric',
}).format(new Date());
---

<header class="site-header">
  <!-- Top utility bar -->
  <div class="topbar">
    <div class="container topbar-in">
      <span class="date">{today}</span>
      <span class="tlinks">
        <a href="/archive/">כל הכתבות</a>
        <a href="/about/">אודות</a>
        <a href="mailto:info@autotimesisrael.com">צור קשר</a>
      </span>
    </div>
  </div>

  <!-- Masthead: centered logo -->
  <div class="masthead">
    <a href="/" class="logo-link" aria-label="אוטו טיימס ישראל — עמוד הבית">
      <Image src={logoLight} alt="אוטו טיימס ישראל" widths={[320, 520]} sizes="440px" />
    </a>
  </div>

  <!-- Sticky nav bar -->
  <nav class="mainnav" aria-label="ניווט ראשי">
    <div class="container nav-in">
      <button class="menu-toggle" id="menuToggle" aria-label="תפריט">
        <span></span><span></span><span></span>
      </button>
      <div class="nav-links" id="navLinks">
        <a href="/">בית</a>
        {CATEGORIES.map((c) => <a href={catHref(c)}>{c}</a>)}
      </div>
    </div>
  </nav>
</header>

<style>
  .site-header {
    background: var(--paper);
  }

  /* Top bar */
  .topbar {
    background: var(--navy);
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.78rem;
    border-top: 4px solid var(--accent);
  }
  .topbar-in {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 36px;
  }
  .topbar .date {
    color: rgba(255, 255, 255, 0.6);
  }
  .tlinks {
    display: flex;
    gap: 18px;
  }
  .tlinks a {
    color: rgba(255, 255, 255, 0.75);
    transition: color 0.15s ease;
  }
  .tlinks a:hover {
    color: #fff;
  }

  /* Masthead */
  .masthead {
    display: flex;
    justify-content: center;
    padding: 22px 20px 18px;
  }
  .logo-link :global(img) {
    height: 235px;
    width: auto;
    display: block;
  }

  /* Nav bar */
  .mainnav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: var(--paper);
    border-top: 1px solid var(--line);
    border-bottom: 3px solid var(--navy);
  }
  .nav-in {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 52px;
  }
  .nav-links {
    display: flex;
    align-items: center;
    gap: 34px;
    font-weight: 700;
    font-size: 1rem;
  }
  .nav-links a {
    position: relative;
    padding: 15px 0;
    color: var(--navy);
    transition: color 0.15s ease;
  }
  .nav-links a:hover {
    color: var(--accent);
  }
  .nav-links a::after {
    content: '';
    position: absolute;
    inset-inline: 0;
    bottom: -3px;
    height: 3px;
    background: var(--accent);
    transform: scaleX(0);
    transition: transform 0.18s ease;
  }
  .nav-links a:hover::after {
    transform: scaleX(1);
  }

  .menu-toggle {
    display: none;
    position: absolute;
    inset-inline-start: 20px;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: 0;
    cursor: pointer;
    padding: 8px;
  }
  .menu-toggle span {
    width: 24px;
    height: 2px;
    background: var(--navy);
    border-radius: 2px;
  }

  @media (max-width: 780px) {
    .logo-link :global(img) {
      height: 157px;
    }
    .topbar .date {
      display: none;
    }
    .topbar-in {
      justify-content: center;
    }
    .nav-in {
      justify-content: flex-start;
    }
    .menu-toggle {
      display: flex;
    }
    .nav-links {
      display: none;
      position: absolute;
      top: 100%;
      inset-inline: 0;
      flex-direction: column;
      gap: 0;
      background: var(--paper);
      border-bottom: 1px solid var(--line);
      padding: 6px 20px 14px;
    }
    .nav-links.open {
      display: flex;
    }
    .nav-links a {
      padding: 12px 0;
      border-bottom: 1px solid var(--line);
    }
  }
</style>

<script>
  const t = document.getElementById('menuToggle');
  const n = document.getElementById('navLinks');
  t?.addEventListener('click', () => n?.classList.toggle('open'));
</script>
